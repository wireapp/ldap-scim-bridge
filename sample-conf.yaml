logLevel: "Debug"  # one of Trace,Debug,Info,Warn,Error,Fatal; `Fatal` is least noisy, `Trace` most.
ldapSource:
  tls: false
  host: "localhost"
  port: 389
  dn: "cn=admin,dc=nodomain"
  password: "geheim hoch drei"
  search:
    base: "ou=People,dc=nodomain"
    objectClass: "account"
  codec: "utf8"
  deleteOnAttribute:  # optional, related to `delete-from-directory`.
    key: "deleted"
    value: "true"
  deleteFromFirectory:  # optional; ok to use together with `delete-on-attribute` if you use both.
    base: "ou=DeletedPeople,dc=nodomain"
    objectClass: "account"
scimTarget:
  tls: false
  host: "localhost"
  port: 8088
  path: "/scim/v2"
  token: "Bearer scim-test-token/team=471a5f6e-86fa-4850-b6e0-9758259afcdf/code=b873fbf9-e3b5-487b-8098-ab1f47fed7b0"
mapping:
  userName: "uidNumber"
  externalId: "uid"
  email: "email"
